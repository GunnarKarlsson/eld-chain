# Changelog

All notable changes to `eld-common` are documented here.

## [0.1.0] - unreleased

Experimental protocol library. Wire format and validation match the current `eld` node and clients; this is not a frozen mainnet spec. Not published to crates.io (`publish = false`).
